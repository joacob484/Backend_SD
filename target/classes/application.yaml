spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/appdb
    username: app
    password: pass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  redis:
    host: redis
    port: 6379

spring:
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest

server:
  port: 8080
